# Configuring EFcore

1. Install required packages(3-4)
2. Connection String in AppSetting.json
    "ConnectionString": {
  "Default": "Server=localhost;Database=Project_Management;Trusted_Connection=True;TrustedConnectionCertificate=True"
}

3. Data Folder
4. DbContext file/class
5. Register DBContext in Program.cs file